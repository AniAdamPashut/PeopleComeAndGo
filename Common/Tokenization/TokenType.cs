namespace Common.Tokenization;

// Do NOT change the order of this enum

public enum TokenType
{
    Keyword,
    Literal,
    Operator,
    Separator,
    Identifier,
    Whitespace,
    Comment,
}

